# docs/24 Part A — 盤中訊號雷達 worker 專用金鑰模板。
# 複製為 pipeline/intraday/.env 後填值;.env 已被 .gitignore 涵蓋,絕不 commit、絕不進 GitHub secrets。
# 完整部署步驟見 docs/vps_backfill_plan.md Step 5(2026-07-12 版)——
# 但該版早於 2026-07-13 Cloudflare Access 上線,缺下面兩行 CF_ACCESS_*,
# 現在 pages.dev 也被 Access 擋,沒這兩行 worker 會在第一次抓 radar.json 就 403 fatal exit。

# Fugle MarketData API key(申請/輪替紀錄見 vps_backfill_plan.md Step 5-b 附近)
FUGLE_API_KEY=

# Supabase(既有專案,與 watchlist 功能共用;service_role key 才能寫入、繞過 RLS)
# Dashboard → Settings → API → service_role
SUPABASE_URL=
SUPABASE_KEY=

# radar.json 來源(cutover 前維持 pages.dev;WP-B3 後改 https://radar.techtrever.com/data/radar.json)
RADAR_JSON_URL=https://trever-radar.pages.dev/data/radar.json

# Cloudflare Access service token(2026-07-13 Access 上線後必填,否則 403 fatal exit)
# 建立:Cloudflare Zero Trust → Access → Service Auth → Service Tokens → Create Service Token
# 建立後還要把它加進既有 Access Application(A0-A2 那個)的 policy:
# Include 規則新增一條 "Service Auth" = 剛建立的 token 名稱,否則 token 建了也穿不透。
CF_ACCESS_CLIENT_ID=
CF_ACCESS_CLIENT_SECRET=
