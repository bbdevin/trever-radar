# Handoff — 2026-09-04（還原因子修復完成、夜間作業靜默死亡歸因、方向 battery 進版控）

## 2026-09-04 最新交接

- **Current Goal**：本輪的主軸從「驗證關鍵分點面板」變成「先把面板底下的資料修對」——今天連續查出三層資料缺陷,而它們全都餵進同一份分析。按依賴順序:
  1. **`daily_prices` 約 15,150 個股票×日缺漏**(見下方 🚨 條目)。修復工具開發中,**執行待人工放行**。在補齊之前,E2 的 20 市場日窗口對受影響股票是跨著空洞算的,方向 battery 跑出來的數字對那批股票不可信。
  2. **`compute-performance --all`**,把已修好的還原因子傳導到 `daily_scores` 的前瞻報酬(2,592 列受除權息汙染)。**待人工放行**。分點排行不需要,它會自己重算。
  3. **驗證 2026-09-05 00:05 那輪**真的產出 `branch_pit_stats` 與 `branch_stock_pctile_counts` 兩張表的列。查表,不要因為 cron 改了就假設成功。
  4. **等 490 日分點回補走完形成半段**——實測 `oldest_filled = 2025-03-05`,490 日窗口(2024-08-28～2026-09-03)中 **369 天已補滿(≥1,500 檔)、121 天仍是薄的(~477 檔)**,而那 121 天**全部落在 battery 的形成半段**。形成半段約一半的日子覆蓋率只有評估半段的 **25%**,且那 477 檔是成交額最大的那批,所以形成半段的被標記 pair 來自系統性偏斜的子集,拿去和全覆蓋的評估半段比存活率與 obs/exp **不是同一個母體**。進度:08-27 為 41%、09-04 為 75.3%,八天 34.3 點 ≈ 每天 4.3 點,剩 24.7 點 → **約 2026-09-10 補完**。在那之前跑 battery 得到的數字不能用來決定面板去留。
  5. **在修好的資料上跑方向 battery**(`radar branch-window-direction-battery`,已進版控並通過獨立驗證)。**往前看那側若 obs/exp 塌到 1.5× 以下,已上線的關鍵分點證據面板要撤。** 這是活的風險,不得假設不會發生。

  以下條目由新到舊。

## 2026-09-02 起的交接

- **Current Goal**：本輪 Confirmed Scope = 勝率稽核後續清單裡的低風險項「branch export 日期修正 +『今日買超』文案／正負淨額一致」，程式與測試已完成並 push；正式站要等下一次 VPS `export-json` 才會反映，本輪未跑正式 DB／export／deploy。
- **權證分點 export 資料日（2026-09-02）**：`_export_warrant_branches` 原本把 `data_date` 和 1d／2d／5d／30d 的窗口 anchor 全綁在 `daily_prices` 最新日，分點晚一輪公布時 1d 桶會全空、payload 卻宣稱資料日是報價日（和 `today.json` 的 `as_of` 同一種錯）。現在先用與主查詢相同條件求「不晚於報價日、在近 120 報價日窗口內」的實際最大權證分點日 `bd1`，以 `bd1` 為 1d anchor、嚴格早於 `bd1` 的報價日推 2d／5d／30d，主查詢加 `b.date <= :d1` 上界；`index.json`／`{stock_id}.json` 的 `data_date` 改報 `bd1`，池內無資料時為 `null` 且直接跳過主查詢（市場檔仍為五個空 timeframe，stale shard 照舊清除）。前端 `WarrantBranchPanel` 的 `data_date` 放寬為 `string | null`、只在非 null 時驗 ISO，避免空池被誤判成索引格式錯誤。
- **分點文案（2026-09-02）**：`BranchTrackView` 表頭「淨買超張」→「淨買賣張」（該表由買超／賣超分頁餵 sign-filtered rows，賣超分頁顯示負值）；`/branch` tab「今日動向」／hint「買超明細」→「最近動向」／「買進／賣出與淨買賣明細」，與標題「分點最近交易日進出」及買進／賣出／淨買賣／佔比四欄一致。sign 著色本來就正確（gross 中性、net 正紅負綠零中性），未動。tab key 仍是 `today`，無 URL 相依。
- **驗證**：targeted `test_warrant_branch_export.py` **3 passed**（新增落後一日、空池兩案）；完整 pipeline pytest **314 passed、70 subtests**；`npx tsc --noEmit`、`git diff --check` 通過。未改 schema、cron、workflow、secrets、評分、門檻、正式 DB。
- **環境陷阱已排除（2026-09-02）**：完整 pytest 現在只要 `cd pipeline` 就能跑，不必設 `PYTHONPATH`。根因是 `test_json_export.py` 兩處 `from pipeline.radar.importer import …` 與其餘測試檔的 `from radar.…` 不一致；已統一，實測 `PYTHONPATH` 空值下 **314 passed、70 subtests**。在 repo root 跑仍會 collection error，那是預期行為。
- **全市場檔資料日（同日第二輪）**：`branches/warrant_branches.json` 改為 `{version:1, threshold, data_date, timeframes}` v1 wrapper，沿用 `today.json` 既有先例。`/branch` 權證分頁加 `normalizeWarrantBranchPayload` 雙讀 wrapper 與舊裸 mapping（舊檔不捏造日期），顯示「分點資料日 X；各區間由該日往前推算」；`WarrantBranchPanel` 的 404 fallback 同樣雙讀，只有快照真的帶日期才顯示。500 萬門檻、排序與兩種檢視未改；`docs/07` 已同步 payload 形狀。程式碼走 Pages（push 後幾分鐘）、JSON 走 VPS export（更晚），新舊組合必然並存，雙讀就是為此。
- **VPS 唯讀查核（2026-09-02 16:07–16:15 +08）**：`~/trever-radar` HEAD 已是 `bf65dd0`——**日更腳本自己會 pull**，所以第一輪的 `data_date`／anchor 修正會由 16:10 `daily-insti.sh` 這條既有排程自然發布；當時正在 `import-warrant-master` 階段並持有 `/tmp/radar-db.lock`。四個既有未追蹤檔仍在、未阻斷 pull。磁碟 free 8.1G／71%，仍低於 20GB gate。分點回補 `backfill-branches --top 0 --days 490` 單實例執行中，guard／supervisor 各一。**未手動 import／export／deploy，未重啟、未改 cron。**
- 🚨 **重大資料缺陷:`adj_factor` 全市場從未計算(唯讀發現,未修,需人類決定)。** 2026-09-02 有報價的 **2,393 檔全部 `adj_factor = 1.0`**,全庫**只有 2330 一檔**有非 1 值;`import_logs` 的 `adj_factor` 只有 **1 次** ok(2026-07-07)——即完成清單那句「已用 2330 實測」;**crontab 與所有 vps 腳本都沒有呼叫 `compute-adjustments`**。成因:欄位以 `DEFAULT 1.0` migration 加入,除 2330 外從未更新;且「週六全市場重算 2026-07-10 停用、改 VPS 跑」——雲端停了、VPS 從未接上。**消費端**:`indicators.py:232-235`、`performance.py:99-100`、`compute_branch_stats.py:264`、`json_export.py:1333/1338` 全都乘 `adj_factor`。**證據**:緯穎 6669 於 09-02 由 7,800→2,610(−66.5%)、寶雅* 5904 於 08-20 −88.8%、00685L 於 07-07 −96.0%,全 adj=1.0;2026-06-01 以來 ≤−3% 的 15,219 筆中 adj_factor 未變者 **15,219 筆(100%)**。**影響鏈**:技術指標→`tech_score`→`final`(技術 20%)→`fwd_1d..20d`→**分點 `win_rate`／`avg_ret5`**→`rank_score`。**今日上線的隔日沖重新定義不受影響**(只用張數),但同表的勝率／平均報酬／排行分受影響。**修復代價**:`compute-adjustments --all` 對 2,494 檔各一次 FinMind 請求、預設 sleep 7.0 → 約 **4.8 小時**,其後須重算指標／performance／branch-stats／scores 再 export;屬 AGENTS 的「正式 DB 全市場重算」,**必須人工確認**。
- **`adj_factor` 修復工具已就緒(commit `81b3a28` + `6ce892b`,尚未執行)**:`vps/scripts/adjust-backfill.sh`,一次一塊、可續跑、**對排程讓路而非排隊**。`flock -n` 搶不到就 exit 0(略過是正常結果);state 檔原子寫,失敗的 chunk **不從 state 移除**,下次原樣重試;EXIT trap 保證回補容器不會被留在 pause。**搶鎖在 pause 之前**——迴圈跑 11 小時會有約 220 次略過、只有約 13 次真的做事,先 pause 等於製造 220 次無謂 pause/unpause,並提高撞上 2026-08-27 那個 guard state-cache race 的機率。
  - **驅動方式**(刻意不進 crontab):`while bash vps/scripts/adjust-backfill.sh; do sleep 180; done`,或手動重複呼叫到印出 `all done`。略過回 0、失敗回非 0。
  - **執行窗口**:平日 **02:30–14:05** 是唯一的長空檔(約 11.5 小時),2.0 小時的總量塞得進去。晚間各間隔不到兩小時,硬塞會讓 chunk 跨進日更起點而使該輪略過。
  - **範圍實測**:2,413 檔(有價格列的 stock/etf);`is_active` 過濾與否結果**相同**,下市標的在 `branch_trades` 為 0 筆,故無遺漏。每檔約 3 秒(`--sleep` 預設是 **1.0**,`cli.py:427`;STATUS 先前寫 7.0 已過時)。
  - **跑完之後**只需 `compute-performance --all`(修 `fwd_1d..20d`,那是餵分點勝率的);branch-stats 與 scores 由 23:30 既有那輪自然重算。歷史 `indicators_daily` 影響低——K 線圖的指標是前端用還原價全歷史算的,存的那份主要供當日評分而當日每天重算。**腳本刻意不接重算鏈**,別把它們綁進去。
- **「關鍵分點」正名:那個徽章從來沒有量過它名字宣稱的東西(2026-09-04,使用者指出)。** 使用者的原話是「有些現有的關鍵應該命名不叫關鍵才對,那是我自己看的觀察券商」。查證屬實且更嚴重:`K1_KEY_BUY` 的觸發條件裡**沒有價格、沒有分位、沒有低買高賣**——`pocket.py:342-351` 取 `tracked_branches` **聯集** `branch_rankings` 中 `rank_score >= 70` 且非隔日沖者,`tracked_buy_trigger` 只判「近 5 日淨買 ≥ 500 張或 ≥ 成交量 0.3%」。純粹是身分與規模。而「關鍵分點」在本專案有使用者定義的另一個意思:某分點在**特定個股**上反覆在相對低點買、相對高點賣,是 per-stock 的價格分位概念,對應 `branch_stock_pctile_counts` 與 `BranchPctilePanel`。兩件無關的事共用一個名字。
  - **命名定案(使用者選定):`追蹤分點`。** 沿用既有詞彙——`BranchTrackView.tsx:195,208` 已經在講「尚無追蹤分點」、資料表就叫 `tracked_branches`。「觀察」被排除:頁面上已有觀察門檻、觀察池、僅供籌碼觀察,再多一個專有名詞會混。**須留意**:該集合並非全是使用者手動追蹤的,`rank_score >= 70` 那半段是系統自動入榜,名字略寬,這是知情下的取捨。
  - **「關鍵分點」定為保留字,目前產品中任何地方都不使用(使用者選定)。** 不掛到證據面板上:那個面板刻意不做二元斷言,因為實測標籤再現率僅 1.6~5.4%,今年合格者明年幾乎全換一批,掛徽章等於暗示永久性;且方向 battery 尚未在修好的價格上跑過,面板本身仍在可撤回清單上。**名字等 battery 判決。**
  - **實作(`9a79fe7` + `998acaa`)**:`key_buy_trigger`→`tracked_buy_trigger`、`KEY_*` 常數→`TRACKED_*`、`PocketContext.key_keys`→`tracked_keys`、family `"KEY"`→`"TRACKED"`(前端只在 `types.ts:73` 有型別宣告、不分支,可直接改)、code `K1_KEY_BUY`→`T1_TRACKED_BUY`、文案「關鍵分點同買」→「追蹤分點同買」。另修掉 `/branch` 排行卡上同一個集合(`rank_score >= 70 || source === "manual"`)的「關鍵」徽章——不在原清單內,但那是同一個錯名,留著等於白改。
  - **雙讀過渡**:程式碼走 Pages(數分鐘)、JSON 走 VPS export(當天匯出輪),必然並存。`PocketBadges.META` 同時掛 `T1_TRACKED_BUY` 與 legacy `K1_KEY_BUY`、都指向「追蹤分點」;`ReasonPill` 接受兩種前綴;`types.ts` family union 保留 `"KEY"`。**另補一項執行者未涵蓋的**:個股頁是 `compact=false`、渲染伺服器產生的 `t.text`,tooltip 亦然,所以舊 payload 會出現徽章寫「追蹤分點」、展開卻寫「關鍵分點同買」。加了 `displayText()` 只對 legacy code 換開頭前綴(分點名稱原樣保留),與 META 的 K1 條目同時可刪。
  - **迴歸守衛**:測試斷言輸出的 tag 的 `code` 與 `text` 中**不得出現「關鍵」二字**。這次的失敗模式就是有人把名字加回去。pytest **465 passed、100 subtests**;`tsc --noEmit` 乾淨。
- 🚨 **`daily_prices` 有約 15,150 個「股票×日」永久缺漏,現有任何指令都補不回來(2026-09-04 唯讀發現)。** 把今天抓到夜間作業的那套「排程活性稽核」推廣到全部排程時查出來的。
  - **兩個洞**:①**上櫃報價 2026-07-16～08-19 連續 25 個交易日**——`import_logs` 每天成對記兩筆,上市 `ok rows≈31,000`、上櫃 `empty rows=0`,一天不漏;`daily_prices` 每日上櫃列數 **~475 對正常 ~985**,即每天缺約 510 檔。08-20 恢復(`ok rows=10,634`),時間點正是 `e58a820` 加入 15:00 上櫃補抓槽。②**2026-08-11／12／13 三天完全沒有 quotes 匯入紀錄**(log 由 08-10 直接跳到 08-14),上市列數 **~565 對正常 ~1,365**,每天缺約 800 檔;同期 `daily_institutional` 為 **0**。那正是 8/21 事故(本地 dirty scripts → `git pull` Aborting)的區間。
  - **為什麼補不回來**:`backfill()`(`importer.py:212-226`)以 `have = SELECT DISTINCT date FROM daily_prices` 判斷,**只要那天有任何一列就 `continue`**。台股報價來自上市／上櫃兩個各自獨立的來源、在同一次 `import_daily(ds,["quotes"])` 內分別記錄,一邊成功另一邊失敗時,那個日期就永遠被當成完成。`backfill --days 60 --datasets quotes` 今天跑會把這 28 天全部跳過。
  - **`deep_backfill` 也救不了**:它以「最早日期 < 2010」當新鮮度檢查跳過已補過的股票,一檔在 6 月被 deep 過的股票,7 月的洞永遠不會被回頭補。缺口中仍有 ~475 檔上櫃有資料,正是那些在缺口**之後**才被 deep 到的。
  - ⚠️ **這個缺陷有人踩過但只繞了一半**:`backfill_margin` 的 docstring 自己寫著「Unlike `backfill()`, checks `daily_margins` completeness per date instead of skipping when `daily_prices` already has the day」——只幫融資券繞過去,報價沒繞。而 `backfill_margin` 自己也不完全:它用絕對下限 `cnt < min_rows`(預設 500),而 2026-09-02 的 `daily_margins` 有 **1,293 列對正常 2,211**(缺 980 檔上櫃＋83 檔上市,來自 21:21:20 那筆 `margin error rows=0` 的半途失敗),1,293 > 500 故永不被標記。
  - ⚠️ **對進行中分析的意義**:E2 的「事件日前 20 個市場日」窗口是由 `date_index` 依實際存在的列建出來的,受影響的股票那個窗會無聲地跨過 25 個交易日的空洞,分位因此失真。方向 battery 在修好之前跑出來的數字,對這批股票是不可信的。
  - **修復開發中**:讓 `backfill()` 改以「每個市場相對於自身常態水準」判斷完整性(不寫死列數,市場集合由 `stocks.market` 推導),並與 `backfill_margin` 共用同一個判準。**執行仍須人工放行**(全市場重新匯入)。
- **`adj_factor` 全市場修復已完成(2026-09-04 07:16)**:driver 跑完 **2,413 檔、0 失敗**,`all done — state file is empty`;過程中 01:01 正確印 `inside quiet window — skip` 讓路,未與任何排程搶鎖。**但修復尚未傳導到分數列**——`compute_performance` 預設的候選條件是 `entry_price IS NULL OR fwd_20d IS NULL`,**已填好的列永遠不會重算**,故跨除權息的窗仍帶著用未還原價算出的 `fwd_1d..20d`。需 `--all`;規模實測 `daily_scores` 僅 **22,361 列／35 個日期**(2026-07-06～09-03),不會重演八月 1.7GB 的記憶體形狀。**已寫好含 `flock -n`／安靜窗檢查／pause bf／EXIT trap／前後 `SUM(fwd_20d)` 對照的一次性腳本,執行被權限層擋下,待人工放行**(與 AGENTS「全市場重算須人工確認」一致,未繞過)。
  - **曝險已量化(唯讀)**:2026-06-01 以來共 **1,504 個除權息／分割事件、涵蓋 1,360 檔**;`daily_scores` 中 **2,592／22,361 列(11.6%)** 的 20 交易日前瞻窗跨越其中至少一個事件,那些列的 `fwd_*` 現在是錯的。最大者 5314 於 08-20 `0.2406 → 1.0`(4.16 倍)、6669 緯穎於 09-02 `0.3353 → 1.0`;以緯穎為例,未還原時該窗算出約 **−66.5%** 的假虧損,還原後接近 0%。
  - ⚠️ **更正 handoff 稍早那條影響鏈的說法**:`compute_branch_stats` **不讀 `daily_scores.fwd_*`**,它自己用 `forward_returns(adj_candles, ed)` 從 `daily_prices` 現算(`compute_branch_stats.py:300` 取 `adj_factor`、`:347` 算前瞻),所以**分點 `win_rate`／`avg_ret5`／`rank_score` 會在下一輪 `compute-branch-stats`(22:00 或 mid-publish)自動用修好的價格重算,不需人工介入**。真正卡在 `daily_scores.fwd_*` 的是 `strategy_performance.py`——即首頁 `strategy_meta` 的 S1–S13 策略績效紀錄。所以 `--all` 修的是「策略到底有沒有用」那組數字,不是分點排行。
- **夜間作業靜默死亡的確切歸因(2026-09-04 補完)**:根因 commit 是 **`e47a175 fix(vps): run margin at 21:20 after TWSE publish`**,它把平日晚窗從 `2055..2200` ＋ `2235..2320` 併成單一 `2115..2330`;`30 23` 這個 cron 從「兩窗之間的空隙」變成「新窗的最後一分鐘」。落點與 log 完全吻合:**08-25(週一)有 done → e47a175 落地 → 08-27、08-28 週日無 done → 08-29(週六)、08-30(週日)有 done → 08-31 起週日全無 done**;週末能跑是因為週六窗為 `55..230`＋`450..730`、週日為 `55..400`,都不含 2330。**正式機當日實測損害**:`branch_pit_stats` 與 `branch_stock_pctile_counts` 皆 **0 列**(兩個指令只存在於這支腳本);分點排行**未受影響**,因為 `mid-backfill-publish.sh` 每天也在跑 `compute-branch-stats`,帳面看起來一切正常。
  - **比排程更深的缺陷**:`safe-branch-stats.sh` 是**唯一只用 `fuser` 看鎖、從不呼叫 `acquire_db_lock` 的寫入者**。其他人(`data-backfill.sh` 01:10、各 `daily-*.sh`)都真的拿 `flock -n 9`。後果有二:①check 之後一秒才啟動的寫入者會與它並行,單一寫入者假設破裂;②它的實際保護只剩時間窗算術,而 `in_radar_quiet_window` 由五支腳本共用、被別人維護——這次就是別人動了窗,它靜靜死掉且只送 default 等級 ntfy。
  - **修法三件**(`fa54fae`):①改拿真鎖(撞上時讓路的是 `data-backfill`,那是冪等可續跑的歷史回補,少一晚無代價);②連續跳過超過 `STALE_HOURS`(預設 30)改送 **high** 等級告警,把靜默失敗變成會叫的失敗;③新增測試把 `crontab.example` 每個時段拿去對撞 `in_radar_quiet_window`,要求至少 5 分鐘邊界餘裕,並以「舊的 `30 23` 必須失敗」作為回歸案。
  - 🚨 **修法①原樣上線會讓整條日更停擺,已在推送前攔下(`993f3d1`)**。`flock` 鎖的是 **open file description** 不是程序;`safe-branch-stats.sh` 收尾會 `nohup` 補起 `bf-cron-guard.sh`／`bf-supervisor.sh` 兩個**常駐** daemon,在持鎖狀態下起,它們會繼承 fd 9 而讓鎖**永不釋放**——此後 14:10／15:00／16:10／17:40／21:20／22:00 每一輪 `acquire_db_lock` 都 `flock -n` 失敗而 `exit 0`,且 `bf-cron-guard.sh` 用 `fuser` 偵測時會看到自己,把回補容器永遠 pause。**比原本要修的 bug 嚴重得多。** 修法是起 daemon 前 `exec 9>&-`(此時 DB 工作已全部結束),先例見 `manual-catchup.sh:36`。
  - 🚨 **同一個洞在 `weekly-tdcc.sh` 已經在正式機上**(非本輪引入):`:32` 拿鎖、`:47/:50` 起同兩個 daemon,中間沒放。平常不觸發是因為 `pgrep` 幾乎總是發現 daemon 已在跑而略過 `nohup`;只要遇上兩個都不在的時刻(重開機後 `@reboot` 尚未生效),週六 06:30 那輪就會把鎖永久留下。已一併修掉。
  - **第五項:`pipeline/tests/test_vps_lock_discipline.py`**——從腳本原始碼判定誰持鎖、誰起常駐子程序,要求兩者之間有 `exec 9>&-`,不維護白名單,新腳本一寫出來就自動納入。另有兩條防解析器失效的檢查。以位元組安全方式移除修復後實測會失敗並指名檔案、兩個行號與後果。pytest **448 passed、100 subtests**。
- **方向驗證 battery 已提交並通過獨立驗證(`106dd85`)**:`radar branch-window-direction-battery --as-of --window-days --seed --out`,唯讀(`get_read_only_sqlite_engine`、`mode=ro`、缺 DB 直接 `FileNotFoundError`、`--out` 先過 `safe_report_output_path` 才查詢),兩個方向同一套標準,含存活率(先算、無評估半段活動者留在分母)、每股二項式虛無、**流量配對安慰劑**(episode 數 ±25% **且** 事件日 `abs(pct)` 中位數 ±25%,兩條件 AND)、lag 測試,以及四條判決行。驗證者實際造出讓 `backward obs/exp = 0.0` 的資料情境並取得 `[WITHDRAW backward]` 判決行——**那條能撤下已上線面板的準則是真的會算、真的會觸發**。obs 與 exp 共用同一個 `_exceeds` 述詞(`probability_exceeds` 內部直接呼叫它),不可能漂開。pytest **436 passed、98 subtests**。⚠️ **尚未對真實資料執行**——數字要等 490 日分點回補跑完才有意義。
- **Fable 5.1 決策:買低賣高的窗口方向(2026-09-04)。** `docs/04` line 64 原規格寫「**其後** 20 日」(往後看),實作卻是事件日＋**前** 19 個市場日(往前看)——**自程式寫成以來就矛盾**。決定:**往前看是定義之記錄、唯一篩選條件、目前唯一顯示的數字**;往後看以「事後 20 日區間分位」獨立名稱被接受,但**必須先通過已提交版控、跑在修好還原價上的樣本外測試**,且**永遠不會把分點帶進面板**。
  - **關鍵發現(主協調先前未察)**:同股基準確實化解了「買到上漲股票就得分」的天真反對,但兩件事活在分點層級、基準吸收不了——**①前 15 大切片本身製造價格衝擊**(每個事件依定義都是淨流量大到擠進前 15 大的日子,集中買盤推動其後 1–5 日價格;往後看時該衝擊落在窗內,把事件收盤推向低檔;基準吸收平均衝擊、吸收不了該分點自己的,而後者與張數成正比。**既有安慰劑以 episode 數配對、非張數配對,從未測到**);**②自我汙染方向翻轉**(週一/三/五都買的分點:往前看時週五窗含自己先前買出的漲幅 →**更難**得分=保守;往後看時週一窗含自己後續買出的漲幅 →**更容易**=寬鬆)。**往後看的偏誤方向正好會「證實」內線理論。**
  - **技術論證**:`adj_factor` 向後累乘,故窗**之後**的除權息讓窗內 20 個收盤同乘常數,**在 min/max 分位中相消** → 事前分位改用還原價後仍只是過去價格的函數,point-in-time 承諾不破。
  - ⚠️ **抓到兩個現成缺陷**:①`_price_rows_for_stock` 讀原始 `close`、**沒乘 `adj_factor`**(indicators／performance 都有乘);②**先前的樣本外驗證從未提交成腳本**,撤回決策無法重現,新 battery 必須進版控。
  - ⚠️ **事先寫死的撤回條件**見 `docs/STATUS.md` 同日條目(張數配對安慰劑、lag 測試、三選一即撤)。**往前看也需在還原價修好後補跑確認**:若 obs/exp 塌向安慰劑(<1.5×),代表先前驗證是未還原區間的假象,**應撤下面板**——這是活的風險,不得假設不會發生。
- 🚨 **夜間分點作業自安靜窗設定以來平日從未執行(2026-09-04 發現,待人工改 cron)**。`safe-branch-stats.sh` 的狀態檔停在 `finished=2026-08-31T00:29:44`,`branch_pit_stats` 與 `branch_stock_pctile_counts` 皆 **0 列**——今日接進該作業的兩個新計算完全沒有產出。**根因是邊界相等**:`in_radar_quiet_window` 平日窗為 `2115 <= hhmm <= 2330`(閉區間),cron 在 23:30:01 觸發 → `hhmm=2330` → 落窗內,log 印 `inside quiet window — skip`。差一分鐘就會執行。log 佐證:8/27–9/03 每天都有 `start`,但只有 8/30 00:26 與 8/31 00:29 有 `done`(前一晚分別是週六、週日;週末窗不含 23:30)。**改 00:05 而非 00:30**:本作業實測 56–59 分鐘、加新計算更久,01:10 是 deep backfill,00:30 必撞;22:00 那輪實測 23:53–00:12 結束,00:05 時鎖多半已釋放,萬一超時被擋的是可續跑的 deep backfill。**`crontab.example` 已更新為 `5 0 * * *` 並寫入理由;正式機 crontab 亦已於 2026-09-04 01:0x 依使用者明示指示套用**(先前 agent 寫入被權限層擋下,停下說明後才由使用者放行,未繞過)。套用時先 `crontab -l` 備份到 `~/crontab-backup-<stamp>.txt`,以 sed 只改該行時間欄位,並在 `diff` 標記數 ≠ 2 時拒絕寫入;套用後行數不變。**首次以新時間執行為 2026-09-05 00:05,必須查驗而非假設。**
- 附帶(未處理):平日的 `safe-branch-stats` 與 22:00 `daily-branches` 在 stats／scores／export／deploy 上大幅重疊——22:00 本來就會呼叫 `compute-branch-stats`;真正只存在於夜間作業的是今日新增的兩個計算。日後可把那兩個移進 22:00 那輪、夜間作業退成週末補算,但需判斷哪一次 invocation 該跑。
- **隔日沖重新定義已在正式站生效並驗收(17:40 那輪)**:`as_of 2026-09-03`、831 個分點,`is_daytrade=1` **3**／`=0` 808／**`NULL` 20**(未判定,改動前會被靜默寫成 False);`matured_samples` 未填 **0**、`samples != matured` **819**。標記者:群益金鼎-大安 67/240、凱基-台北 544/1960、元大-三峽 29/113——比率與 08-28 副本量測略異(當時凱基-台北 0.3152、現 0.2776)**屬預期**(as_of 不同且回補持續改寫歷史),三個名字與順序不變、全在 0.20 以上。pair 層 `suspect=1` **2,433**、`NULL` **825,732**(這 82 萬個未判定,改動前全被當成「不是隔日沖」)。匯出正確:`rankings.json` 三個新欄位齊備,主榜 **828**／隔日沖榜 **3**,NULL 正確留主榜。
- **`b83c51e` 重構了 `compute_branch_stats.py`**(觀察建構與計數抽成共用函式),經逐項核對為**行為保留**,並**修掉當日稍早放行的瑕疵**——pair 層計數原是回吐規則的第二份複製,現已合一。pytest **400 passed、98 subtests**(主協調自行執行)。該重構約 19:30 推送,正式機隔日 14:10 才 pull,**今晚 23:30 仍走已驗證的舊版**。
- **pair 層隔日沖證據已接上 UI(`b83c51e`)**:`branch_pctile_counts` 新增 `min_daytrade_obs` 與 pooled `stock_daytrade_obs`／`stock_daytrade_paybacks`,每個分點增 `daytrade_obs`／`daytrade_paybacks`。**刻意不 join `branch_stock_stats`** —— 它用全部歷史、pctile 表用 490 交易日,併在同一張卡會讓讀者以為同期;改為在同一模組、同一窗口一起算。面板第三列為「次日回吐」,三態分明:欄位缺失→不渲染、觀察數不足→**明示「無法判定」並列出門檻**、其餘→完整計數＋該股基準刻度＋帶號 pp。
- **關鍵分點證據面板已實作(後端 `b715e88`＋前端 `f377235`;今晚 23:30 產生首份資料)。** 使用者選定「證據呈現版」——傾向為真但標籤再現率僅 1.6–5.4%,故**不做二元徽章,只呈現計數與尺**。後端:新表 `branch_stock_pctile_counts`(PK `branch_name, stock_id`,`create_all` 建立,**只留最新快照**),六個 pair 計數＋**把該股自身四個計數反正規化到每列**當尺,**全表無 rate/score/flag/rank/win/return 欄位**(有測試掃 `PRAGMA table_info`),未加 `prune.py`。**與 Fable「不做分點×個股表」的決議不衝突**:那條排除的是 945k×250 as_of ≈140GB 的**時間序列**,本表是當下一張快照(約 90 萬列、數十 MB),差三個數量級。計算為 stock-major 串流(`yield_per=2000`、逐股 flush),合成壓測 441 萬列/549MB DB 下 `tracemalloc` peak 3.3MB、整體 74.6MB(計算本身約 +11MB),記憶體只隨每股分點數成長。匯出:個股 JSON 加 `branch_pctile_counts`,兩側各 ≥5 次、上限 10 個分點、依超過該股比率的幅度排序,**不輸出比率**。接線於 `safe-branch-stats.sh`,同 `SKIP_PIT` 形狀、失敗不中止、`SKIP_PAIR_PCTILE=1` 可關。
- **前端** `BranchPctilePanel.tsx` 掛在個股「籌碼」分頁 `BranchFlowSection` 之後(資料已在 payload 內,不另 fetch)。每側呈現 `28 / 33 次 84.8%` ＋ 0–100% 軸(實心條=該分點、刻度=該股基準)＋ 帶正負號 `±Npp`——只給兩個百分比要人心算,那把尺就白給。**文案紅線全部落實**:無「關鍵分點」斷言、無「總是」、無名次、**無紅綠方向色**(會讓計數讀起來像判決)、無損益字眼;明寫「窗口內累積的紀錄,不是今日名單」與實測門檻(多數股票幾張即可進前 15、熱絡個股數十張,故計數是活動量下限);頁尾「兩側各自獨立計數,沒有配對成交易,不能讀成賺賠」。舊 payload 安全:`version!==1`／非物件／`branches` 非陣列一律 `return null`,`rate()` 分母 ≤0 或命中>分母回 null 而非 0%。pytest **397 passed、96 subtests**、`tsc` 通過;`verify-mobile-stock.mjs` **不適用未執行**(需 playwright,repo 未安裝,且只驗身份區)。
- ⚠️ 面板資料要等 **23:30** 那輪跑完並 export 才會出現,在那之前是誠實空狀態。**`adj_factor` 修完後此量測值得重跑**——20 日分位目前建立在未還原價上,除權息與分割會扭曲分位。
- ⚠️ **事故與復原(2026-09-03 15:00,已修復)**:14:58 手動啟動 `import-themes`,而 **15:00 就有 `daily-tpex-quotes` 排程輪**;守衛只檢查絕對上界(15:45 之後不跑)、**沒檢查距下一輪還有多久**,且預估 15–25 分鐘實際跑 35 分鐘未完。15:00 那輪 `flock -n` 搶不到鎖而略過,今日 TPEx 行情一度 0 筆。**資料未損**(themes 是 staged 完才單一 transaction 寫入,中止不留半截;確認仍 832 stale／6,869 列)。**已修復**:手動補跑 `daily-tpex-quotes.sh` → `tpex quotes ok rows=10994`、今日上櫃 1,013 檔;在它進入指標／分數／export 前停止,把鎖讓給 16:10 的 `daily-insti`(今日法人**無後續補跑**);鎖已釋放、回補 15:49:30 UNPAUSE 運行中。**教訓**:同日稍早自己 commit 的 `adjust-backfill.sh` header 已寫明「02:30–14:05 是唯一長空檔」「長跑抓著鎖會害當輪略過」——理由寫對、執行時仍在兩分鐘縫隙裡啟動長作業。**正確守衛是「距下一輪剩餘時間 > 預估耗時」,不是絕對上界**;長爬蟲一律排 02:30–14:05。
- ⚠️ **題材仍待執行**:完成條件已修(`c6f4e22`)且在正式機上,但尚未成功跑過,`themes` 全數 stale、個股「活躍題材」仍不顯示。下週一 09-07 排程會自動以新規則跑;要更早就併進 02:30–14:05 與 `adj_factor` 回補一起做。
- **關鍵分點(per-stock 低買高賣)可行性量測完成:傾向為真且通過樣本外,但標籤不可重現。全程唯讀、未實作。** 原料早已存在於 E2 shadow(`low_buy` = 買進 episode 分位 ≤0.40、`high_sell` = 賣出 ≥0.60,本來就在分點×個股層級算)。**關鍵前提:兩側基準不對稱**——全市場 pooled 低買 **53.35%**／高賣 **35.35%**,故「兩邊 ≥0.5」在買側幾乎免費(obs/exp 1.07–1.45×,噪音),須丟棄。樣本內 490 日、以每檔股票自身 pooled 率為 null:`n≥10 且兩邊 ≥0.7` → **361 對／291 檔／7.05×**,該 361 對全部在兩側勝過自身股票基準。**樣本外時間切分(決定性)**:形成 2024-08-27→2025-08-28、評估 2025-08-29→2026-09-02;存活 74–86%;兩側都勝 **67.3%／62.1%／56.1%**、obs/exp **2.94×／2.56×／2.34×**、中位超額買 +13.9～+27.1pp。**規模配對安慰劑(最可能推翻的對照)通過**:同股、樣本數 ±25%、未標記者 obs/exp 僅 **1.12–1.24×**、超額 +0.4～+2.0pp;兩側都勝 67.3% vs 29.5%(z≈3.9)。**篩到的不是曝光度或部位規模。**
- ⚠️ **但再標記率僅 1.6%／3.6%／5.4%**(安慰劑 0–0.6%)——**直接牴觸使用者定義裡的「總是低買高賣」**。傾向為真、「總是」不成立;今年合格者明年幾乎全換一批。**設計含意**:不做暗示永久性的二元徽章,改**呈現證據與分母**(「買 33 次其中 28 次在低四成;賣 20 次其中 14 次在高四成;該股基準 57.2%／31.9%」),措辭為「歷史上在這檔股票買點偏低、賣點偏高」,與隔日沖徽章同標準。**全部是價格分位、不是損益**,`docs/37` 禁止配對與獲利歸因。
- ⚠️ **回補完成後須重做此量測**:評估半段已知 episode 比形成半段多 **60%**(346 萬 vs 216 萬),因 490 日回補未填完舊資料,故 flag 數 64／140／368 為低估。其餘限制:單一切分與單一市場區間;`n≥10/0.7` 僅 55 個存活樣本;`branch_trades` 為前 15 大切片,「兩側各 10 筆」本身先是集中度篩選(安慰劑中和了比較,未中和入選資格)。
- **可重複的稽核方法(今天三個問題都靠它抓到)**:把 `cli.py` 所有 `add_parser("…")` 子命令與 **`vps/scripts/*.sh`** 比對,找出「指令寫好但沒有任何排程呼叫」的功能。**別把 `.github/workflows/*.yml` 算進去**——那 5 支資料 workflow 已退役無觸發源,卻會讓子命令看起來有被引用,`compute-adjustments` 第一次就這樣被漏掉。結果:34 個子命令、10 個未被在跑的腳本呼叫,其中 7 個(三支 shadow、兩支報表、`init-db`、手動的 `import-buybacks`)**設計上就是手動工具,非缺口**。反向驗證:`branch-point-in-time-persist` 未出現在清單中,因為今天剛接進 `safe-branch-stats.sh`。
- 次要發現:①**`import-stock-info` 最後成功 2026-07-07、僅 2 次**,2,494 檔 active 中 **19 檔無產業別**,且**新上市個股將永遠沒有**(產業別餵首頁族群資金流與個股頁)。②`stocks.description` **0/2,494**,但**前端沒有任何元件渲染它**(只在 `web/lib/types.ts:385` 有型別),故為休眠程式碼路徑、非可見缺口;STATUS 完成清單的「股票卡…新增公司基本業務說明」宣稱不成立,推測是 `docs/37` B 的 `company_profiles` 取代後未回頭修文件。
- ⚠️ **題材匯入「永遠不可能成功」的缺陷已修(commit `c6f4e22`,尚未生效)**。**先前文件記載的「`import-themes` 尚未執行」是錯的**——它每週一都有跑(`daily-market.sh:15`),7/08 起每週 ok 約 6,869 列;真相是 **8/31 起註定失敗**。根因:完成條件要求 `empty == 0`,而 8/31 為 `failed=0, empty=230`——抓取全成功,只是 1,062 類中 230 類沒有成分股(白酒、煙草、槍枝、麻紡、葡萄酒、皮革製品、樂器……富邦分類法通用,台股本來就沒有)。`empty == 0` 是 8/27 lifecycle 那批帶進來的,**只要清單裡永遠有空分類就永遠失敗**。後果:832 筆題材全 `stale`、`data_date`／`source_updated_at` **0/832 從未寫入**(只在成功路徑寫)、**個股「活躍題材」自 8/31 起完全不顯示**。修法:`empty` 不再阻擋完成(它是已觀測到的事實,真正異常會計入 `failed`),改為 `len(staged) + empty == len(theme_list)`;另加 `THEME_EMPTY_MAX_SHARE = 0.5` 擋「來源整體壞掉、每頁回空」(實測 22%,距門檻兩倍餘裕)。以 8/31 數字驗算新規則會 complete。pytest **381 passed、74 subtests**。
- ⚠️ **仍待執行:必須手動跑一次 `import-themes`** 才會生效(排程只在週一,否則等 9/07);將在 14:10 日更後、鎖釋放時執行。已知並接受:空分類若先前有成分股,舊 `stock_themes` 會保留——刪掉等於做「缺席即退役」的推論,正是該模組要避免的,且該分類維持 `stale`,前端不會顯示。
- **庫藏股正式機首次匯入完成(2026-09-03 12:21)**:授權後以 `flock -n` 取 DB 鎖執行 `import-buybacks --as-of 2026-09-03 --days 366`,前置檢查(無日更程序／鎖未持有／free 8.0G／MemAvailable 1,294MB)通過。結果 **23 個計畫、23 檔**(上櫃 13／上市 10),`board_date` 2026-08-05～08-24,`report_date` 2026-09-03,log 記 ok。**`buybacks` 表首次有資料**(先前 0 列)。`--days 366` 但只回到 8/05,是因 MOPS `t35sc09` 給的是**執行中**計畫而非一年歷史,`execution_pct` 為 None 亦正常。**未手動 export／deploy**,交由 14:10 日更自然上線。
- **決策一已實作,三項上線前量測全過(commit `b933ab3`,未在正式 DB 執行)**:新表 `branch_pit_stats`(PK `branch_name, as_of, window_market_days`)**全表無 rate 欄位**,每個分子帶分母與 unknown,`fwd5_sum_pct` 存總和;新 CLI `branch-point-in-time-persist --as-of [--window-days]`;`prune.py` 註明**永不清理**。記憶體約束以 **stock-major 串流**解決(不呼叫會保留 913k episode 的 report builder,改按 `stock_id, branch_name, date` 排序 `yield_per`,價格切片用完即丟)。**量測(對還原出的 4.8GB 正式副本實跑)**:①peak WS **63 MB**、每 as_of **31–50 秒**,對照 `compute-branch-stats` 的 1.7GB OOM 差約 **27 倍** → 通過;②5 個 as_of 寫 4,104 列、成長 155,648 bytes = **38 bytes/列**、每 as_of **30KB**、**7.3 MB/年**,比 Fable 估的小約 7 倍;③離散度 p10 0.4941／p50 0.5546／p90 0.6037,**p90−p10 = 10.95pp**(Fable 的門檻是 3pp)→ **該表確有區辨力,日後可上 UI**。912,015 個 buy episode 中未知分位 **221,963(24.3%)**,與序列量到的 23% 一致,再證「只存 rate 藏起四分之一資料」。每 as_of 寫 821 列,非交易日 **fail closed**(2026-08-22 週六被正確拒絕)。pytest **372 passed、74 subtests**。順帶修掉既有效能缺陷:`_price_observation` 原本每個 episode 重算一次 `sorted(market_index)`(913k 下為 O(n·m log m)),改為可傳入預算好的查表,不傳則行為逐字相同。
- **已排入夜間作業(使用者授權,commit `818f0eb`)**:折進既有 `safe-branch-stats.sh`(23:30),**不新增 cron、不新增第二個寫入者** —— 該腳本已做完安靜窗／DB 鎖／mid-publish flag／磁碟／記憶體五道守衛與 backfill pause,獨立排程等於重寫一遍並遲早漂移。位置在 `compute-branch-stats` **成功之後**、`compute-scores` 之前;**失敗不中止本輪**(沿用 `compute-scores` 的 `set +e`／捕捉 rc／warn 後繼續),結果寫入 `$STATE_FILE` 的 `pit=`,並有 `SKIP_PIT=1` 可關。**預設 as_of 取 `daily_prices` 而非 `branch_trades`** —— `plan_as_of_window` 要求 as_of 存在於 `daily_prices`,若分點先落地而行情匯入失敗(8/31 TPEx 520 即此類),沿用分點慣例會每晚失敗;無可用交易日則 fail closed。pytest **376 passed、74 subtests**,`bash -n` rc 0(WSL bash 5.2.21)。
- ⚠️ **歷史回補待執行**:決定先 seed **60 個交易日**(約 40 分鐘)。回補列**不具備證成持久化的那個性質** —— `computed_at` 誠實標示它們是「今天對過去的看法」而非「當天看得到什麼」,價值弱於每日新增列;60 日等於窗口長度,序列即刻可用,日後可加深(重跑取代該列、可中斷續跑)。執行方式:`flock` 取同一把 `/tmp/radar-db.lock`、暫停回補容器、完成後恢復,紀律比照 `safe-branch-stats.sh`,不新增檔案。
- **決策二已實作並端到端驗證(commit `0821f04`,未在正式 DB 執行)**:`DAYTRADE_MIN_OBS` 4→8、新增 `DAYTRADE_MIN_PAIRS=20`／`DAYTRADE_PAIR_SHARE=0.20`;`daytrade_flag` 不足時回 `(None, None)`;`is_daytrade_suspect` 未判定寫 **NULL** 並新增 `daytrade_obs`／`daytrade_paybacks`;`_BranchAgg` 改 pair 計數、`is_daytrade()` 回 `bool | None`;`branch_rankings` 增 `matured_samples`／`daytrade_pairs_determined`／`daytrade_pairs_flagged`;`AUTO_IN` 抽成 `auto_in_blocked_by_daytrade()`,**只被 `is True` 擋**。**匯出端真 bug 已修**:detail-set 的 `AND is_daytrade = 0` 會靜默丟掉 NULL 列,改 `COALESCE(is_daytrade, 0) = 0`;主榜切分改顯式 `!= 1`／`== 1`。前端三個新欄位全 optional,`effectiveSamples()` 套用四處,徽章顯示「隔日沖 615/1951 檔」／「隔日沖未判定」,tooltip 明寫**比例是下限**。
- **端到端驗證(對還原出的 4.8GB 正式副本實跑 `compute-branch-stats`)**:五個欄位的 additive migration **在真實生產形狀 DB 上成功**;結果與獨立精確重算**逐項吻合**——`is_daytrade=1` **3 個**(凱基-台北 615/1951=0.3152、群益金鼎-大安 57/197=0.2893、元大-三峽 23/80=0.2875)、pair `suspect=1` **2,236**、`daytrade_obs>=8` **164,569**、`determined>=20` **799**、落選者 0.1799／0.1569／0.1500 全同。內部一致:2,236+162,333=164,569;938,177−164,569=**773,608 個未判定 pair**(過去靜默寫 False)。**819 個分點的 `samples != matured_samples`**,證明混淆真實存在且已分離。`is_daytrade NULL`=32 vs 腳本的 28 非分歧(腳本只走 827 個有合格日的分點)。完整 pytest **363 passed、70 subtests**、`tsc` 與 `git diff --check` 通過。
- `branch_ranking_v2_shadow` 的歷史門檻**刻意凍結**(它是本決策的稽核紀錄):`daytrade_flag` 加 opt-in `min_obs`,線上走預設,shadow 用自己的 `V1_DAYTRADE_MIN_OBS = 4`。`branch_rankings.style` 對未判定仍寫 `"swing"`——與 `is_daytrade_suspect` 同屬「寫了沒人讀」(前端僅型別宣告、無渲染),**刻意不動**;若日後 style 要上 UI 須一併處理。
- **使用者已批准正式上線(2026-09-03 02:3x +08)**——此為 `AGENTS.md`「仍須人工確認:schema migration」的人工放行,不是自動 push 的延伸。批准當下 VPS HEAD 為 `d3949f5`(01:10 `data-backfill` 拉的純文件),實作尚未進正式機。時序:**14:10** `daily-market.sh` pull 後由 `init_db()` 在正式 `radar.db` 跑五個 `ALTER TABLE ADD COLUMN`;**17:40** `daily-branches.sh` 的 `compute-branch-stats` 使新定義生效並 export／deploy(**23:30** `safe-branch-stats.sh` 亦重算)。可見變化:3 個分點被標記並移入隔日沖清單、32 個顯示「未判定」。回滾 = `git revert` ＋ 下一輪 cron,新欄位 additive 留著無害,舊快照 NULL 即版本標記。**本輪未手動在 VPS 執行 import／export／deploy／migration,未改 cron。**
- ⚠️ **尚未做**:決策一的 `branch_pit_stats` 計數帳與串流持久化模組(同樣會改 `schema.py`,故分開)。
- **Fable 5.1 決策內容（決策紀錄）**。它另查出三事、我方已獨立驗證：①`is_daytrade_suspect` **寫了沒人讀**（全 repo 僅 schema 欄位、`compute_branch_stats.py:367` 寫入、今日文件）；②分點層級布林值 gate 了主榜／隔日沖切分、detail set 的 `AND is_daytrade = 0`、`AUTO_IN` 三處，今天全是 no-op；③E2 report 把每個 episode 留在記憶體（單 as_of 約 913k），與 **2026-08-25 OOM** 同形狀（`vps/README.md:219`、`compute_branch_stats.py:199`、`crontab.example:45` 皆佐證），故持久化**不得**照原樣呼叫 `build_branch_point_in_time_report`，必須串流累加。
  - **決策一**：E2 只在**分點層級**持久化為 additive table `branch_pit_stats`（PK `branch_name, as_of, window_market_days`），**只存整數計數、永不存 rate**，每個分子都帶分母與 unknown 計數，存 `fwd5_sum_pct` 而非平均；約 200KB／as_of、50MB／年，**永久不 prune**。不做分點×個股表（~140GB）、不做市場表（讀取時 pool）。持久化理由:490 日回補正在改變過去日期的查詢結果，帶 `computed_at` 的列是「當天看得到什麼」的唯一紀錄。
  - **決策二**：分點層級隔日沖旗標保留但**改以「已判定 pair 中被標記的比例」**定義，退休 pooled 回吐比率（涵蓋約 1,128 檔股票的分點不可能總體達 60%）。pair 層級 min-obs 改 8、不足時寫 **NULL 不寫 False**，`branch_stock_stats` 增 `daytrade_obs`／`daytrade_paybacks`；分點層級用 `daytrade_pairs_determined`／`daytrade_pairs_flagged`，**分母必須是已判定 pair**。`AUTO_IN` 只被 `is_daytrade=1` 擋，NULL 不擋。成熟度級距兩層都不採用，改在 `branch_rankings` 增 `matured_samples`、前端徽章讀 `matured_samples ?? samples`（即 shadow 讀法 (a)）。
- **校準已定案（Fable 第二輪）**：`DAYTRADE_PAIR_SHARE = 0.20`、`DAYTRADE_MIN_PAIRS = 20`、`DAYTRADE_MIN_OBS = 8`；`determined < 20` → NULL，`>= 20 且 share >= 0.20` → 1，其餘 0。**關鍵詮釋:0.33 是可觀測性上限而非行為上限**——pair 只有在該分點擠進當日該股前 15 大賣方時翻單才可見，凱基-台北兩種分母下都只到 0.31–0.32，那是資料天花板。故徽章語意必須是「**可判定 pair 中、在前 15 大可見範圍內次日翻單的比例**」，**不可說成「多數時候翻單」**。0.20 高於 p99(0.12)、約 p90 的 8 倍；選 0.20 而非 0.15 是刻意保守，因 share 與分母無論如何都會顯示在卡上（元大-竹科 80/408 仍可見），而偽陽性會逐出主榜並擋 `AUTO_IN`。20 是讓常數不被機率淹沒的最小分母:base rate 約 1.5%，min_pairs=10 時 `P(>=2/10)≈0.94%`×800 ≈ **7.5 個偽陽性**，min_pairs=20 時 `P(>=4/20)≈2.4e-4` ≈ **0.2 個**（此推算已由主協調獨立驗算）。
- **上線前驗收已通過(2026-09-03 精確重算,102 秒,唯讀)**:min_obs=8 得 determined **164,569**／flagged **2,236**(base rate **1.36%**);`determined >= 20` 者 799 個、落 NULL 者 28 個;share p50 0.0000、p90 0.0213、p99 0.1221、max **0.3152**。**`share >= 0.20` 恰好標記 3 個**:凱基-台北 615/1951=0.3152、群益金鼎-大安 57/197=0.2893、元大-三峽 23/80=0.2875 → 符合「2–10 且含凱基-台北」,**常數照案實作**。最接近落選者:元大-竹科 84/467=0.1799、凱基-宜蘭 8/51=0.1569、群益金鼎-台北 9/60=0.1500。交叉驗證:min_obs=4 的 flagged 精確值 5,120 與既有欄位的 5,118 幾乎相同;determined 164,569 大於代理指標的 139,076,與「代理偏嚴格」一致。
- 歷史備註(原文保留):須以真實規則精確重算（observations = 合併前合格買超日、determined 為 `>= 8`、flagged 為回吐比率 `>= 0.6`）。**驗收規則:flagged 落在 2–10 且含凱基-台北才可照案實作;不符不得自行調整常數,須帶數字回送 Fable。** 實作時另須在 `docs/13` §8 寫入上述徽章語意、可觀測上限約 0.33、常數源自 2026-08-28 分布,並**撤回**「min-obs 8 是 no-op」說法附上 5,118→2,100。
- **先前的校準實測(初擬 0.5 會標記 0 個分點)**，等同重現它要修的缺陷。實測（831 分點）:分母＝全部 pair 時 p50 0.0018、**max 0.3117**（凱基-台北 615/1973），`>=0.5` **0 個**、`>=0.3` 1 個；分母＝`events_count>=8` 的 pair 時 p50 0.0000、**max 0.3433**，`>=0.5` **0 個**、`>=0.3` 3 個、`>=0.1` 12 個。另 **pair 層級 min-obs 4→8 不是 no-op**:pair 池縮到 14.83%、flagged 5,118→2,100（-59%）——**先前「改 8 零代價」只在分點層級成立**，不得引用為 pair 層級證據（此更正由 Fable 指出）。代理指標偏保守（`events_count` 是合併後事件數，故 `observations >= events`；既有 flag 在 min-obs 4 下算）。常數已回送 Fable 重訂，**未定前不得實作**。
- **E2 穩定度序列真實資料結果:全粒度持久化不可行。** 對還原副本跑 9 個 as_of、60 交易日窗口，涵蓋 821 分點與 **945,131 個分點×個股實體**，輸出 **5.07 GB** JSON。歷史回算約 250 個 as_of 同粒度將達 **~140GB** 級，而 VPS 僅 29G／free 8.0G，零成本原則又排除需綁卡的代管儲存。市場層級 rate 穩定到近乎常數（`low_buy_rate` 53.283864% → 53.292160%，差 0.008pp；`high_sell_rate` 34.392680% → 34.075746%），**穩定但資訊量極低**。unknown 質量很大：913,078 個 buy episode 中 fwd5 未成熟 **226,356（25%）**、缺已知 20 日分位 **212,140（23%）**——證實裸 rate 會藏起四分之一資料。
- ⚠️ 持久化粒度／欄位設計，以及分點層級隔日沖訊號的存廢，已依使用者指示交 **Fable 5.1** 決策（`AGENTS.md` 禁止單一模型自行拍板 schema）；結論落定前不得實作。
- **排行 V2 真實資料結果:原本兩個「待人工確認」的問題被證明是空的。** 對 WP-B4 還原出的 2026-08-28 正式副本執行（本機唯讀，未碰正式 DB）：831 個分點的 `matured_samples` 中位數 **3,708**（min 5、p90 7,975、max 132,015），落在 `<10` 僅 **1** 個、`<30` 僅 **5** 個——`docs/13` §8 的 10／30 門檻比中位數低三個數量級。三種讀法 `rank_drift` 全為 **0**，(a) 831 列／(b) 831 列 830 評分／(c) 830 列。隔日沖 4 vs 8 **零差異**（`unknown_min8=0`、`verdict_differs=0`），且兩者標記數皆為 **0**；正式快照 `branch_rankings` 2026-08-28 的 `is_daytrade=1` 同為 0，證明工具無誤（8/21 及更早也僅 1）。V1 的 samples 混淆真實存在（819/831 有落差、平均 91）但僅佔分母約 **1.7%**。
- ⚠️ **新發現才是真問題**：`branch_stock_stats.is_daytrade_suspect` 在**分點×個股**層級有 **5,118/937,856（0.55%）** 為真，該層級 `events_count` 平均僅 **4.97**。**門檻在分點×個股層級才有意義，套在分點層級會被稀釋**（一個分點平均涵蓋約 1,128 檔股票）。前端 `MIN_SAMPLES=10` 徽章同理，正式資料上只對 1 個分點生效（samples min=7）。
- **據此已可定案兩題（有數據、非取捨）**：①「成熟樣本 <10」建議採 **(a) 評分並標記**——榜單集合與 V1 完全相同、只多一個誠實標記，代價為零；②隔日沖最低觀察數 **改 8**，與 `docs/13` 原規劃一致、實測零影響，消除文件與程式的長期不一致。不改公式／權重／`final`。**仍待人類決定**：分點層級的隔日沖訊號是否還該存在，以及門檻是否改在分點×個股層級重新校準——那是產品語意，不是實測能直接推出的。
- **兩支唯讀 shadow 工具已合併（決策證據，未改 schema、未寫 DB）。** `branch-ranking-v2-shadow --as-of --out` 同時量化「成熟樣本 <10 不評分」的三種讀法（(a) 評分但標記／(b) 列榜但 score·rank 為 null／(c) 不列入），各報進退榜數與名次漂移；隔日沖並列 V1 的 4 筆與 `docs/13` 原規劃的 8 筆門檻，**V1 觀察數不足時回 `False`（未判定卻預設為否），V2 記 `unknown`**。回吐比率沿用既有 `daytrade_flag`，兩側同一決勝規則以免漂移來自 tie-break。`branch-point-in-time-series --as-of-from --as-of-to --step --window-days --out` 把 E2 單日 shadow 沿多個 as_of 推進彙總；窗口以**市場交易日**計、彙總由 `episode_samples` 重算而非平均各日 rate、同時輸出 pooled 分子分母、單一觀察 stdev 為 null、缺席日明列不前向填補。**對 schema 決策最關鍵的觀察:同一 `low_buy_rate` 各日平均 75% vs pooled 66.7%，差距純來自分母加權——只存 rate 不存分母的 schema 不可還原。** 完整 pytest **350 passed、70 subtests**；V2 作者 agent 因 session 額度中斷未跑套件，其程式由主協調審核（唯讀 engine、無 schema import、無寫入）後才合併。
- ⚠️ **待人類決定（三項，勿由 agent 代選）**：①「成熟樣本 <10」採 (a)／(b)／(c)；②隔日沖最低觀察數維持 4 或改 8；③E2 是否落 schema、若落是否連分母一併存。另注意兩工具的 fixture 為合成極端案例，能證明彙總會如實呈現不穩定與缺口，**不能**給出真實量級（真實分母是 3 還是 300、真實波動多大），那需要對非正式環境的真實 DB 副本跑過。
- **DB 瘦身可行性（唯讀實測，結論是「不要做」）**：起因為「權證會過期，資料能否移除以瘦身」。方向合理但資料不在那裡。年齡型 prune 一直在跑（`disk-cleanup.sh` 每日 07:40，`warrant_daily` 150 交易日，穩定刪約 45,000 列／日），該表 5,765,963 列僅約 0.5GB，改按到期刪最多再省 ~0.2GB。`branch_trades_raw` 22,920,549 列中權證列只佔 1,482,024（6.5%），**早於 120 日窗口界（2026-03-11）的權證列只有 10,529 列（約 1.6MB）**——全市場權證分點近期才開始收集，沒有累積。無廢表（`branch_trades` 已是 view，migration 無殘留）、**`freelist_count=0` 故 `VACUUM` 回收不到任何空間**。質量是窗口外的**股票**分點列 14,779,714（64%，約 2.3GB），而那是 `--days 490` 回補刻意在建的資產。**可回收總量 0.2GB 級，對 8.4GB 需求是雜訊；Phase 2 只能靠更大磁碟或更短窗口，沒有瘦身路徑。不實作按到期日清理。**
- **WP-B6／M4 容量分析（唯讀）：20GB gate 在現有硬體上不可達。** 磁碟 29G／已用 19G／free 8.0G；`radar.db` 5.52GB＋WAL 98MB、`web/` 894M、`cloudflare-data-worker/` 201M、`/swapfile2` 2.0G、Docker images 3.02GB。`docker system df` 可回收合計 <210MB，清理救不了。要 free 20G 等於全機用量須 ≤9G，光 DB 就 5.5G——**不是「等使用者按下去」，是硬體滿足不了門檻**。且 `docs/30` §5 自己寫明舊估計已作廢、須先跑 1／5 日 PoC 量測成長率，**20GB 只是等待量測期間的佔位值，沒有實測支撐**。另注意 WP-M4 **Phase 1 其實正在跑**（VPS 上的 `backfill-branches --top 0 --days 490` 即是），被擋住的只有 Phase 2 權證全市場 120 日。**使用者授權後已取得實測（全程唯讀、未寫入）**：`WARRANT_BRANCH_DRY_RUN=1` 唯讀目標報告得 2026-09-02 全市場 **19,109 檔**（TWSE 15,513＋TPEx 3,596），較 `docs/30` §5 估的 ~12,000 **低估 37%**；以 `--sleep 1.0` 計一日 ≈ 5.3 小時、120 日 ≈ 2.29M 請求 ≈ **26.5 天連續執行**。bytes/row 以既有基準回推：`branch_trades_raw` 21,522,284→22,801,922（+1,279,638）、DB 5,324,414,976→5,524,545,536（+200,130,560），得 **≈156 bytes／列**（上界，期間他表成長一併計入分子；`page_count×page_size` 與檔案大小相符）。以 8/31 那輪 2,619 目標→61,687 列的比率（≈23.6 列／目標，取自高成交額池、對全市場偏高）推，120 日 ≈ 5,410 萬列 ≈ **8.4 GB > free 8.0 GB**；砍半估也要 4.2 GB、跑完剩 3.8 GB，仍遠低於 gate 的「完成後 ≥10GB」。**結論：不是門檻太嚴，是工作量在 29G 碟上放不下。** 可行形狀為縮短窗口（30 日 ≈ 2.1GB／6.6 天，60 日 ≈ 4.2GB），或擴充磁碟才做 120 日。寫入型 PoC **未執行**——唯讀量測已足以判定 120 日不可行；若決定跑縮短窗口，第一日的實際寫入本身就是最好的 PoC。待人類三選一，皆未執行。
- **週備份鏈唯讀稽核：舊判斷是錯的，備份一直正常。** Drive 有 6 份快照，最近一次週六排程備份為 **8/29 05:12 成功**（`radar-20260829.db.gz`，1,162,065,971 bytes）。`weekly-backup.sh:18-22` 在 `integrity_check != ok` 時就 `exit 1`、不上傳，所以快照存在即證明那輪檢查通過，不必再補跑。先前三個「症狀」全是誤判：主機無 `.db.gz` 是 `:29` 上傳後 `rm -f` 的正常行為；log 無痕跡是正常路徑幾乎不輸出；8/22 的 Drive 403 不是持續阻斷（當天 05:10 快照存在，且 Drive 總量 5 TiB、已用 13.376 GiB、free 4.987 TiB）。最新快照另做不落地串流驗證：`rclone cat | gunzip -c` 全程不寫磁碟，`gunzip` exit 0、header 為 `SQLite format 3\0`、解壓 4,819,320,832 bytes，37 秒，磁碟前後同為 8.0G free。**後續 agent 不要再把「備份狀態不明」當待辦。**
- **WP-B4 還原演練已完成（2026-09-02，專案首次，在本機做、對正式機零影響）**：取回 `radar-20260829.db.gz` 1,162,065,971 bytes（與 Drive 大小逐位元相符），解開 4,819,320,832 bytes。`quick_check` **ok**、完整 `integrity_check` **ok**（231.8 秒）、`foreign_key_check` **0 違規**、`page_count×page_size` 精確等於檔案大小。內容:22 表、stocks 2,491、daily_prices 10,205,766、branch_trades_raw 18,380,545、warrant_daily 5,729,141、daily_scores 19,341、tracked_branches 47，各表最新日一致為 2026-08-28。與本檔 8/31 稽核紀錄交叉核對:`daily_prices`／`warrant_daily`／`daily_scores` 列數完全相同，`branch_trades_raw` 較少是回補持續寫入所致。**備份鏈至此為端到端已驗證。** 該副本另作為兩支 shadow 工具的真實資料基準，用完即刪，**不是第二份正式資料、不得回寫 VPS**。
- **VPS 金鑰改走 `--env-file`（使用者授權後執行，commit `f8ae814`）**：`lib.sh` 的 `radar()`／`radar_timeout()` 與 `bf-supervisor.sh` 的 `start_job()` 不再用 `-e KEY=值`，改為現產 `mktemp` 0600 檔以 `--env-file` 傳入、呼叫一回來即刪、EXIT trap 兜底。值來源仍是既有 `vps/.env`／`pipeline/intraday/.env`，**不需在 VPS 預先建檔**，所以沒有順序風險。實測該機 `/proc` 未掛 `hidepid`，原本任一本機 uid 都能讀走兩把金鑰。VPS 實測：host 值與容器內值長度＋sha256 前綴完全一致（197／`75b2d82a…`、100／`5208eb09…`），`-d` 分離容器亦同；新 argv 金鑰命中 0；`bash -n`（bash 5.1.8）通過；測試在 `/tmp/secfix` 進行、掛零 volume、只跑 `python -c`，未碰 DB／lock／cron，VPS checkout 未動。離開碼語意以 `( exit "$rc" )` + `return "$rc"` 逐字保留，`daily-insti.sh` 的 exit 75 分支不受影響。`vps/README.md` 已記機制。
- **`--env-file` 已在正式 cron 路徑驗證通過**：17:40 那輪自行 pull `bf65dd0..4bc4712` 後照常完成 twse quotes 34,034／tpex quotes 10,813／twse insti 1,312／tpex insti 889／indicators 185 列／分點池 2,789 targets；以 stdin 傳樣式（金鑰不進 argv）掃 `radar-cron.log`，兩把金鑰命中數皆 **0**，無殘留 `/tmp/radar-env.*`。
- **金鑰不輪換：2026-09-02 使用者定案。** 該 VPS 單一操作者、只有使用者本人能登入，argv 可讀的第二個對象實際不存在；已知悉本次修正只堵未來通道、不撤銷過去可讀狀態，仍選擇不重簽。**後續 agent 不要再列為待辦或自行執行。** repo 端確認乾淨：兩個 `.env` 都在 `.gitignore:28`，git 全歷史無此二檔，無 tracked `.env`，暴露面從未進入 public repo。兩檔權限已由 `644` 收為 `600`。
- ⚠️ 另案未評估：主機的 `portainer`／`appsec-agent`／`npm-attachment`／`watchtower` 若有 `--pid=host` 或掛 host `/proc` 者仍可見 argv；portainer 持 docker socket 即等同 root。

## 2026-08-31 最新交接

- **Current Goal**：分點排行 bounded detail union、2026-08-31 上櫃日 K 手動補抓與日常分點的上市權證 100 萬成交額門檻均已正式發布／同步並驗收。TPEx 520 結構化重試與 16:10 安全降級亦已完成、通過 Luna High review 並同步正式機程式碼；今日端點穩定性與真實 520 降級路徑仍待自然事件驗證。
- **TPEx 520 安全降級（2026-09-01，已同步正式機程式碼）**：`RadarHTTPError` 攜帶 status／URL／attempts／原始錯誤；`dailyQuotes` 僅在從第一次起全為 520 時才總計五次（5／10／20／40 秒＋0–2 jitter）。任何 502／timeout 一出現即維持普通三次、線性無 jitter，後續 520 也不延長，最後失敗不多 sleep。`_run` 對有 status 回 `error_kind=http`／`status_code`，status-less timeout／connection 回 `transport`；JSON／parser／DB 不會偽標。`import-daily --datasets quotes` 必須剛好唯一 TWSE ok + 唯一 TPEx HTTP 520 的兩筆結果才 exit 75。`daily-insti.sh` 唯一接住 75：仍跑 insti／主檔後明示本輪不發布，跳過 aggregate／compute／export／deploy，留給 17:40；主檔失敗只提示重試，其他 rc 高優先失敗且不報成功。動態 shell harness 的 fake `lib.sh` 啟用 `set -euo pipefail` 與本地 ERR record trap，驗 75／insti failure／主檔失敗／正常路徑。targeted **29 passed、12 subtests**；完整 pipeline pytest **312 passed、70 subtests**；`bash -n`、`git diff --check` 通過；Luna High 最終 **APPROVE**。commit `e9ce054` 已 push，13:13 +08 在無鎖／無日更程序且 tracked clean 下保留四項 untracked，由 `1e11345` ff-only 同步 VPS 至 `e9ce054`。**未手動執行正式 DB／import／export／deploy，未改 cron／workflow／secrets。**
- **G-RO 報表唯讀契約（2026-08-31，程式／測試完成）**：`phase2-diff-report` 與 `phase3-strategy-performance-report` 已移除 `init_db()`／一般寫入 engine，僅接受既存實體 SQLite、驗 header 與必要表後以 URI `mode=ro` 執行 `SELECT`。`--out` 會拒絕 DB、`-wal/-shm/-journal` 的精確路徑及既有 symlink／hardlink alias；不存在、非 SQLite 或缺表均 fail closed。為正確讀取 active WAL，SQLite 可能更新 `-shm` 的 reader-lock/read-mark 協調 metadata；DB／WAL 內容不會被報表修改。fixture 覆蓋 active WAL 最新列、SQL-only、無 DDL/DML／migration、輸出路徑與 sidecar alias；完整 pipeline pytest **278 passed、58 subtests**，Luna High review **APPROVE**。**未跑正式 VPS 報表／DB，未動 cron。**
- **F-STATE 權證分點續跑契約（2026-08-31，Luna APPROVE）**：`backfill-warrant-branches --state-file BASE` 是 optional；不給參數保留 legacy 行為，給定時 BASE 僅作命名種子，每一 `(date, market)` 寫獨立 atomic `stem-YYYY-MM-DD-market.json`，避免全市場×120 日單一巨型 state。state 用 date＋market＋target hash 分 scope；`ok`／合法 `empty` 跳過、`error`／`pending` 重試、pool 改變只重置該日期，既有 DB rows 視為 ok。只要存在未完整日期即 fail closed（`resume required`、error log、CLI nonzero）。明確 state path 在 `init_db`／`get_engine` 前拒絕 DB、sidecar exact 路徑與 symlink／hardlink；寫入採同目錄獨占 temp＋flush/fsync/replace，建立／替換前重驗最終路徑，舊惡意 `.tmp` 不會被觸碰。個股權證分點顯示 `index.data_date`，並如實說明只涵蓋「已匯入且符合條件的權證／依已匯入池」；100／500 萬與每檔前 15 大限制不變。完整 pipeline pytest **288 passed、58 subtests**、`npx tsc --noEmit`、`git diff --check` 通過。**未跑正式 VPS PoC／DB／export，未改 cron；free 7GB <20GB gate，禁止自行啟用全市場權證輪。**
- **日常分點權證成交額過渡池（2026-08-31，已同步正式機）**：`import-branch-trades` 保留 `warrants=200` 的 legacy 預設與既有呼叫；新可選 `--warrant-turnover-min N` 指定後，改取標的是 active 普通股的當日 **TWSE** 認購／認售且 `turnover >= N`（含等於；`0` 合法、負值 fail closed），明確優先於、不疊加 legacy Top-N。TWSE 限制的是權證 market，標的可為 TWSE／TPEx 普通股；ETF／inactive／未映射標的排除。`daily-branches.sh` 現傳 `--top 0 --warrant-turnover-min 1000000`；此上市過渡池不是 TPEx 全市場獨立輪。targeted **13 passed**，完整 pipeline pytest **296 passed、58 subtests**，Luna High review **APPROVE**。先等 17:40 舊輪 29,467 rows／0 failed 且 Worker=`36874b4b-492e-4862-92a7-66ee69b1933a` 完成、lock 釋放，再保留既有 untracked、ff-only `1fefb09 → ae1ad45`；正式腳本已驗新參數生效。同步未手動跑 DB／import／export／deploy，未改 cron／schema／workflow。
- **分點最近交易日進出（2026-08-31）**：`branches/today.json` 現為兼容 wrapper `{version:1, as_of, movements}`；`as_of` 取價格日以前 tracked 分點的實際最大交易日，沒有 movement 則 null／空 mapping。`/branch` 新／舊 payload 都可讀：新檔顯示資料日，舊 bare mapping 不捏造日期；欄位是買進／賣出／淨買賣／佔比，gross 中性、net 正紅負綠零中性、null 佔比為「—」。exporter targeted **11 passed**；完整 pipeline pytest **290 passed、58 subtests passed**；`web npx tsc --noEmit`、`git diff --check` 通過。未跑正式 VPS／DB／export，未改 cron／workflow、schema、評分或 threshold。
- **分點排行 bounded detail union（2026-08-31，已正式發布）**：`branches/track` 現包含 tracked 分點 + 最新排名非隔日沖 Top100（按 `rank_score DESC, samples DESC, branch_name ASC`）的 union；同名 tracked source 優先，ranking-only 保留 candidate／auto。三層 hard cap=Top100／200 branches／20k rows-shard；tracked 自身超過 200 即 fail closed，ranking 只填剩餘額度。每 shard 只取 bounded names、120 日且不晚於 export date；index/shard 的 loading／error／ready-empty 分開呈現。使用者授權後，VPS 保留所有未追蹤檔並受控 ff-only 至 `591c09e`，只跑唯讀 export＋data deploy；Worker=`ca3eff26-680c-4e14-81ca-d3accde31a21`。正式 index 138；`華南永昌-大安` candidate shard=4,824 rows、83 交易日、2026-05-04～2026-08-28，已登入正式站點入驗收。驗證：targeted **16 passed**；完整 pytest **295 passed、58 subtests passed**；`npx tsc --noEmit`、`git diff --check` 通過；GitHub Pages deploy run `33369139944` 成功。未改 DB／cron／workflow／tracked seed。
- **2026-08-31 上櫃鎖／520 與手動恢復**：14:10 `daily-market.sh` 跑至 15:11，15:00 `daily-tpex-quotes.sh` 因 DB lock 安全略過；16:10 與後續三次手動完整腳本均因 `dailyQuotes` HTTP 520 fail closed。520 response 為 Cloudflare SJC，16-byte body；同 URL／IP 在分鐘內交替 200/520，不是 429 或 SQLite 問題，能確定 edge/origin 路徑間歇異常，但無供應商內部 log，不能斷言更深層原因。獲使用者授權後，以同一官方 URL 的 curl 長退避取得 payload，驗 date/stat/19 fields/10,713 rows 後交既有 parser＋transaction 匯入；權證彙總 845、指標 5,078、scores 750、export 2,410 stocks、Worker=`51b690a4-9b50-407d-b981-1d6c26e9533c`。正式 DB 8/31 TPEx=888 stock／119 ETF／6 ETN／1 other；正式站 6488 行情日 2026-08-31。temp／lock 清理、guard 自動恢復完成；中止唯讀 SQL 留下的單一 orphan process 已精確終止，writer／回補服務未受影響。未改 cron／code／workflow。
- **UI final（HEAD `8603f3a`）**：`8603f3a` deploy success。個股為代號／名稱分離、44×44 Watchlist 右上、價格在名稱下方、行情去框；Decision 固定完整顯示（無收合 button，四 pills 可見），觀察／失效價在行情下方。行情摘要的開高低有方向時才顯示 `▲`／`▼`＋紅／綠，持平／缺昨收時為中性色且無 glyph。個股 8 tabs：K線／籌碼日報／三大法人／資券／大戶／基本資料／技術／權證；首頁 10 tabs：綜合／策略／未發動／已發動／資券／市場掃描／追高風險／失效／口袋／權證。ThemeToggle 已上線。
- **正式站證據**：in-app browser 390×844，`clientWidth=375 / scrollWidth=375`；`stock-context-grid=347/347`、header `202/202`、price `154/154`、market `135/135`；基本資料已驗地址、股務、來源、3 題材、庫藏股誠實空態，無 console error。來源／最終實作／比較歷程與 `final result: passed` 在根目錄 `design-qa.md`。先前 `f323f95` 的「Decision 預設收合」是歷史紀錄，已被本輪明確覆寫。
- **VPS 2026-08-31 12:16–12:26 +08（唯讀）**：alias 為 `trever-vps`；本機/VPS main `8603f3a`。cron 已見 14:10/15:00/16:10/17:40/21:20/22:00、01:10、03/09/12/20、23:30、TDCC Sat06:30、董事 monthly16 07:00。`radar-bf-branches`／`radar-worker` 活躍，各一 guard/supervisor；權證 done=`2026-08-27T00:25:33+08`。分點為長 in-flight（319 日期、最後完成 2025-05-12、fetched=116891；03:56–12:26 無完成行但 DB 持續成長），**不得重啟**。
- **VPS 風險／下一步**：DB 5.32GB／WAL115MB／free7.0GB（75%），writer 活躍故 integrity 與最新 weekly backup 結果 unknown。VPS 有未追蹤 `data/`、`package-lock.json`、`radar-quick-catchup.sh`、`run-backfill.sh`，曾阻斷 pull；不得刪除、pull、重啟或動 cron。free <20GB，禁止自行啟用全市場權證。若使用者另行授權，先唯讀確認 weekly backup+integrity 與回補 completeness／ETA。
- **VPS 13:05–13:06 +08 唯讀續查**：分點回補仍單實例，最後完成 `2025-05-09`、`fetched=118,264`、至少 `320/490` 日期；DB `5,324,414,976` bytes、WAL `115,298,232` bytes、可用約 7.0GB（75% 使用），近期未見 import error。最新成功 backup／integrity 尚無證據，8/22 log 有 Drive quota 403，主機未見 `.db.gz`；dirty tree 仍阻擋 pull。**未重啟、未改 cron、未執行正式 DB。**

## 歷史交接（2026-08-28，已由上列 UI 現況覆寫）

## 2026-08-28 最新交接

- **個股頁最新 browser annotation 已部署並完成正式站 QA（HEAD `f323f95`）**：身份區第一列固定「代號＋完整名稱」（3376 新日興），第二列為「現價＋漲跌點數（漲跌幅）」、第三列市場／產業；44px 自選星號與第一列同高，身份內容在返回鍵右側。行情摘要固定首屏右側，開高低用方向 glyph＋紅／綠／中性色；Decision 首要判讀完整換行且展開不推移行情。重複 compact 概況列維持移除，基本資料題材 compact 與絕對 HTTP(S) URL guard 均保留。390×844（client 375）、深／淺色、Decision 展開／收合與無水平 overflow 已在已登入正式站驗收；參考圖、第一輪與最終畫面同輸入比較無待處理 P0／P1／P2，詳見 `design-qa.md`。`npx tsc --noEmit`、`git diff --check`、deploy 通過；Terra High 實作、Luna High 最終唯讀 review APPROVE。未改資料/API/export/globals/KChart/pipeline/schema/workflow。

- **`55beda9`／`c26ea04`／`f323f95` 個股首屏 UI 均為歷史基準**：舊「名稱與報價同行」格式不再適用；identity 三層、嚴格當日活躍題材 2+N、八個一級 tab、只調水平 `scrollLeft` 與 `scrollY=0` 修正仍有效。**Decision 預設收合已由上列 `8603f3a` 廢止**。`BasicInfoPanel` 的公司資料／題材／庫藏股三 section 及地址／股務／來源／庫藏股／集團資訊均保留；勿依舊截圖恢復被移除的概況列。

- **以下為 2026-08-28 歷史，已由 8/31 日常 100 萬池同步覆寫**：全市場 `import-warrant-branch-trades --market all` 仍是 code-ready、未排程的 PoC；其 `--top` cap／state retry 契約仍有效，但不代表正式全市場輪已啟用。
- 日常 `daily-branches.sh` 現已用上市、active 普通股標的認購／認售 `turnover >=100萬`，不再是 legacy `--warrants 200`／Top200；8/31 22:00 成功 2,619 targets／61,687 rows／0 failed。上市＋上櫃全市場 PoC 仍受 20GB free-space gate、DB/source lock、benchmark 與人工確認限制，未加 cron。
- 本輪驗證：targeted pytest **12 passed**；完整 pipeline pytest **273 passed、58 subtests passed**；`compileall` 與 `git diff --check` 通過；PoC script 與 `lib.sh` 已透過 SSH stdin 交由 VPS `bash -n -s` 唯讀解析，兩者 exit 0（未 pull、未寫 DB、未動 cron）。

- 個股 UI 已補開高低收、量額／資料日與 compact 公司概況；點概況切至既有「基本資料」一級 tab。完整公司地址、股務代理電話／地址、官方來源、題材 lifecycle／來源與庫藏股 MOPS 事實仍保留；活躍題材仍須 `eligible + active + heat_date=quote_date`，沒有放寬。
- 個股權證分點不再被全市場 500 萬門檻清空：export 保留 `branches/warrant_branches.json`（全市場 `>=500萬`），另產出 `branches/warrant-stock-details/index.json` 與 `{stock_id}.json` 分片（個股 `>=100萬`）；100–499 萬只標「觀察」，500 萬以上「大額」。個股 code deploy 早於下一次資料 export 時會誠實 fallback 舊 500 萬檔。W5、首頁／Armed 2,000 萬與 `/branch` 均未改。
- `vps/scripts/daily-insti.sh` 已改成 quotes→insti→best-effort 權證主檔→當日權證彙總→indicators→scores→export/deploy；修正原本 master 在 aggregate 後執行造成當輪新權證 mapping 未反映。正式 crontab 仍是 16:10，沒有新增腳本／cron。
- 新測試 `pipeline/tests/test_warrant_branch_export.py` 覆蓋 2M detail-only、6M 兩檔皆有、<1M 皆無，以及五個 timeframe 與排序。完整 pipeline pytest `263 passed, 58 subtests passed`；TypeScript 通過；Terra 回報 Next build 通過。主協調重跑 Next build 在 Windows 長時間無新輸出後中止，不重複宣稱第二次成功；repo 未安裝 `playwright`，手機 verifier 已嘗試但無法執行，未做 QA bypass。
- **歷史限制（已部分過時）**：`import-themes`、`import-buybacks`、DB migration／全市場回算仍未跑，也未手動改 cron；但日常 100 萬權證池與其 export/deploy 已於 8/31 成功，不能再稱「下一輪才會產出」相關資料。全市場 PoC 仍未執行。

## 下一對話可貼上

```
你是 Trever Radar Executor。必讀 AGENTS.md、docs/project-context.md、docs/STATUS.md、handoff.md、docs/37；依任務再讀 docs/20、docs/22、docs/04。
S4 V2、Armed A1 與 A2 code contract 已完成並通過測試。A2：綜合榜嚴格 `final>=65`（不再不足15保底；同分依 branch_score、再成交額）；S12 無 `concentration_avg20`／基期<=0 fail closed；strategy_meta 為版本化 status/effective_date/rationale/decision_ref/version。lifecycle v2（2026-08-27）依使用者恢復觀察決策把 S2/S5 設為 Shadow、回主要策略選擇器；不代表有效，未改公式、權重、`final`、selector cap 或 DB。舊 JSON 缺 metadata 前端完整 fallback。正式 DB 尚未回算，不可改 schema/workflow/VPS/adj_factor。
B+C+D 已完成程式/fixture/UI：`company_profiles` additive 官方公司欄位、題材 lifecycle、舊 SQLite runtime additive migration、個股 `industry/company_profile` safe fallback；C 只有完整來源成功才 active，partial／empty／`--limit`／失敗保留舊資料 stale、不自動 retired，TTL 35 日；個股 `company_themes/recent_theme_heat` 的 stale／retired／unknown、報價日不一致或未來資料均不得產生 H1／「近期可能相關題材」。官方華新麗華集團 mapping (`1605/2344/2492/5469/6116`)→`groups.json`/個股 `company_groups`→次要 `/group?id=`。群組摘要不依賴 radar pool。**2026-08-27 16:58 已受控正式 VPS `import-geo` 1,985、股務代理 1,985／1,985、3376 驗證及 `export-json` 2,410，Worker version `b377bc68-3c19-42eb-86f5-4e3c20d977d4`；回補 pause 後已 resume，備份為 `/home/huang/geo-before-import-20260827-1658.sql.gz`。**`import-themes`／`import-buybacks`、正式 DB migration／回算、排程/workflow/secrets 均未執行；不可自行執行。
個股 UI 已依使用者確認整併：一級順序為 K線／籌碼日報／三大法人／資券／大戶／基本資料／技術／權證；公司資料、題材、庫藏股在「基本資料」以連續三 section 顯示，沒有內部分頁。題材每筆連續列保留 name/status、分類日、來源更新日與來源（缺值誠實顯示，來源連結達 44px）；名稱區只會顯示最多兩個「活躍題材」+N，條件必須同時是 `eligible=true`、`status=active`、熱度日等於報價日；stale／retired／unknown 或日期不一致資料只在面板如實顯示。集團鑽取、官方資料日、MOPS 事實與舊 JSON fallback 均保留；純前端，未動資料契約。
E1 KB1 code 已完成：官方 MOPS `ajax_t35sc09` POST redirect→短效 `mopsov` HTML，兩步都帶 browser User-Agent／MOPS Referer；stdlib parser 只接受含 `出表日`（相容 `出表日期`）的正式 20 欄表（序號＋代號＋名稱…＋KB1 link 忽略…＋未完成原因），ROC→ISO、空值不補 0；數字序號的正式候選列即使代號空白／損壞也會整批 fail closed，不會部分匯入。`buybacks` additive table 用 deterministic plan ID 保留同股多計畫；`import-buybacks --as-of YYYY-MM-DD --days 1..366` 是手動 CLI，sii/otc 都成功才同一 transaction upsert，任一失敗只記 error log、零資料寫入。export／KB1 都要求 report/source 更新日不晚於資料日；KB1 只在 N 且期間 inclusive 時加既有 BUYBACK 15 分，不改 final／分項／H1；個股 compact 區只呈現 MOPS 事實，無 KB2／執行分點。**E1＋pocket＋JSON targeted pytest 45 passed、3 subtests；`import-buybacks` 未跑、未加排程或改 workflow/secrets。16:58 `export-json`／data Worker deploy 只發布既有快照，未更新庫藏股官方來源資料。**
E2 唯讀 shadow contract 已完成：`python -m radar branch-point-in-time-report --as-of YYYY-MM-DD --from YYYY-MM-DD --to YYYY-MM-DD --out PATH` 只接受既存實體 SQLite，以專用 `mode=ro` engine SELECT；DB 不存在或 out=DB 均 fail closed。輸出固定 JSON 的 universe／coverage、branch×stock rows、**獨立**買／賣 episode、unknown、low-buy／high-sell 與描述性 fwd5；市場日曆合併、20 日分位不讀未來、前15大缺列不當作沒有賣出。**不做 buy→sell 配對或配對 coverage，該規則 deferred 待另確認。**未跑正式 DB/VPS、未接 UI、未定門檻；不做交易獲利歸因或勝率宣稱。
Codex Multi-Agent V2 執行偏好：Sol high 做整體架構／複雜跨模組決策，Terra high 做一般實作／整合／驗收，Luna 做搜尋分析／簡單修改／測試／文件，最終 Code Review 固定 high；Luna 能做不用 Terra、Terra 能做不用 Sol，獨立工作平行、dependency 依序，spawn 明確 model override。此偏好不取代 Cursor Grok/Auto 流程或角色模型中立原則，可由使用者當次覆寫。
正式 VPS 2026-08-27 查核：SSH alias 是 `trever-vps`（不是 `trever_vps`）。16:57 實測 repo HEAD 為 `2b0de0c`、tracked clean；不要在分點回補活躍時自行 pull／migration／import。正式 crontab 已掛 TDCC 週六 06:30 與董監每月 16 日 07:00。權證回補已完成：`bf-warrant.done=2026-08-27T00:25:33+08:00`，`warrant_branch_hist` 12,644 rows／status ok。歷史分點尚未完成：最後完成 2025-10-29、累計 fetched 23,156；以 490 交易日目標估算目前走過 201 日（約 41%），仍餘約 289 日，這只是日期走訪進度、不是資料完整率，舊日期缺口較大，勿線性推 ETA。DB 4.3G＋WAL 91M、磁碟餘 7.9G。14:10 TPEx empty、15:00 10,629 筆成功。已清掉兩個殘留診斷 shell，並安全重啟 guard/supervisor；分點容器在 14:05–15:45 安靜窗內為 `paused=true`，15:46:05 自動 `UNPAUSE`，15:46:11 實測 `running|paused=false`，無 duplicate／restart。其後 16:58 受控完成 geo import/export（1,985／代理 1,985／1,985／3376 驗證／2,410 JSON／Worker `b377bc68-3c19-42eb-86f5-4e3c20d977d4`）及 data Worker deploy，回補已 resume；該 16:58 歷史事件當時未做程式碼 deploy、migration 或全市場重算。17:34 依使用者要求快速正式發布：VPS ff-only 至 `9d1dd69`，從新版 source-controlled `_build_strategy_meta` 僅原子更新 `radar.json.strategy_meta`（未重算／未改 DB／未全量 export），驗證 S2／S5 Shadow、version2、`retired_count=0`；data Worker version `d4f7df6a-dcaf-40be-b033-3c9a901971cb`。cleanup 已移除 flag／釋放 lock 並恢復回補；17:35:50 發現 cleanup 在 guard 記錄 `STATE=paused` 後 unpause，guard 未重驗 container 的 state-cache race，container 仍 running；17:36 已手動 pause 並確認 `paused=true`。本輪未改 guard 程式，19:30 應由既有 guard 依 state 自動 unpause，待後續驗證；GitHub Actions Pages code deploy 成功。
```

- **Done**: S4 V2 兩階段、S4 phase JSON／首頁標示；Armed A1 匯出契約；A2 綜合榜、S12 fail-closed 與 versioned strategy lifecycle contract；lifecycle v2 已依使用者決策將 S2/S5 恢復為 Shadow（JSON export targeted pytest **9 passed**）；docs/37 B+C+D 公司資訊、題材 freshness 與集團鑽取；E1 MOPS KB1 code／fixture／個股 UI；E2 唯讀 shadow contract。E1＋既有 pocket／JSON export pytest **45 passed、3 subtests passed**；完整 pipeline pytest **262 passed、58 subtests passed**；`npx tsc --noEmit` 與乾淨快取 `npm run build`（12/12 static pages、2/2 export）通過。repo 沒有 `npm run typecheck` script，勿把該指令記為驗收結果。目前工作站 `pipeline/.venv` 已依既有 `requirements.txt` 安裝 `supabase 2.31.0`；非阻塞環境債仍是 Node 20 未來不受 `@supabase/supabase-js` 支援。**16:58 正式 geo import/export 與 data Worker deploy 已完成；`import-themes`／`import-buybacks`、正式 DB 回算及 E1/E2 官方來源資料更新均未跑。**

- **2026-08-27 個股 UI（legacy QA 紀錄）**：當時已完成「基本資料」tab 整併、題材列的分類日／來源更新／來源可稽核顯示，以及名稱區活躍題材嚴格顯示；題材 URL 以「查看來源」呈現，保留 href、完整 title 與 aria-label。當時 verifier 使用 iPhone 13 descriptor／390×838；**目前已由 2026-08-28 最新交接取代，verifier 固定 375×812**。工作站仍未安裝獨立 `playwright`；最新正式站視覺與互動證據以本檔頂部及根目錄 `design-qa.md` 為準。未改 API／JSON／pipeline／schema／評分／VPS／workflow，勿把 UI 調整視為正式資料 import/export 授權。

## 勝率稽核後續（2026-08-27，唯讀）

- 已完成策略／分點勝率定義與資料鏈稽核；未改排名、schema 或正式資料，未回算。
- ~~低風險待另案：branch export 日期修正與「今日買超」文案／正負淨額一致~~ **已於 2026-09-02 完成**（見本檔頂部）。正負淨額著色原本就正確，實際修的是權證分點 `data_date`／窗口 anchor 與兩處誤導標籤。
- 高風險待確認：排行 V2（`events_count`／`matured_samples`、成熟門檻、隔日沖定義與 point-in-time shadow diff）；需人工確認後才改 schema／正式回算。
- **前次 Done**: margin cron **21:20**；branches 第二輪 **22:00**；08-26 已 catchup
- **Branch**: `main`

## S4 V2／A1／A2 後續 Confirmed Scope（2026-08-27）

- 總規劃已落檔：`docs/37_company_theme_group_buyback_branch_plan.md`；`docs/27` 已同步 G3 分期與 KB2 決議，`docs/STATUS.md` 已同步狀態。
- A1 程式／測試完成：state list ID 可由 `radar.stocks` 解析；stale warrant 不作今日 state source；缺 1 日／5 日漲幅時 fail closed。**尚未正式 DB 回算**。
- A2 code-level single definitions 已落地並文件化：綜合榜 `final>=65`（同分 branch_score→成交額）、S12 基期 fail-closed、state 同日／缺值 fail-closed、策略／分點 win 口徑、strategy lifecycle v2（S2/S5 Shadow）。正式 DB 回算、排行 V2、schema 與任何門檻／權重調整仍需另案人工確認。
- B 公司地址／股務代理、C 題材 lifecycle 與 D 集團 mapping 的程式／fixture／UI已完成；E1 MOPS KB1 code／fixture／UI 已完成。16:58 已受控完成 VPS `import-geo`／`export-json`（1,985、代理 1,985／1,985、3376 驗證、2,410 JSON、Worker `b377bc68-3c19-42eb-86f5-4e3c20d977d4`）；`import-themes`／`import-buybacks` 尚未執行。E2 point-in-time shadow CLI／JSON contract（獨立 buy／sell，無配對）已完成且測試通過；schema／歷史回算仍需人工確認。
- **KB2 `BUYBACK_BRANCH` 明確不實作**；E2 **不做交易獲利歸因**。不得將舊版 KB2 規劃復活成推測徽章。
- 下一位 Executor 先讀 `docs/37` §9；A2、B、C、D、E1 KB1 與 E2 shadow code 已完成，接續需先取人類確認的 E2 schema／歷史回算或其他 Confirmed Scope。B/D geo 與既有 JSON 已於 16:58 受控發布；C 題材 `import-themes` 與 E1 庫藏股 `import-buybacks` 仍不得自行執行，故該次 export 未更新其官方來源資料；不得改 workflow、VPS 排程、正式 DB 或執行全市場回灌。
